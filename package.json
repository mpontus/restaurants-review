{
  "name": "restaurants-review",
  "version": "1.0.0",
  "description": "Restaurants review website built with NestJS and React",
  "repository": "https://github.com/mpontus/restaurants-review",
  "author": "Michael Pontus",
  "license": "MIT",
  "scripts": {
    "postinstall": "lerna bootstrap",
    "start": "lerna run start --parallel",
    "build": "lerna run build",
    "lint": "lerna run lint",
    "heroku-postbuild": "npm run build --scope server"
  },
  "devDependencies": {
    "lerna": "^2.11.0"
  }
}
